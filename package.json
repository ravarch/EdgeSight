{
  "name": "edgesight",
  "version": "1.0.0",
  "description": "Synthetic Monitoring & Visual Audit API on Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.14",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250307.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.100.0"
  }
}
